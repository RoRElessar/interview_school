<%= form_with(model: student, local: true, data: { turbo: false }) do |form| %>
  <% if student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(student.errors.count, 'error') %> prohibited this subject from being saved:</h2>

      <ul>
        <% student.errors.full_messages do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :first_name %> <br>
    <%= form.text_field :first_name %>
  </div>

  <div class="field">
    <%= form.label :last_name %> <br>
    <%= form.text_field :last_name %>
  </div>

  <div class="field">
    <%= form.label :birth_date %> <br>
    <%= form.date_field :birth_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
